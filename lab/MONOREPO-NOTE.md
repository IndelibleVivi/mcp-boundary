# Monorepo note

This tree was imported from `IndelibleVivi/mcp-app-production-fieldlab@5a6deebbac96089588658452a00f2c52bad0dd2f` for the MCP Boundary unified-workspace candidate.

It remains an executable, neutral evidence laboratory inside the MCP Boundary repository. Passing its checks proves only the named local specimen and receipt boundary; it does not establish arbitrary production or named-host behavior.

This subtree is not a separate repository, not a second installation path, and not part of the distributed plugin. The root `../AGENTS.md` owns the repository contract, `../src/skills/mcp-boundary/` is the one active distributed skill, and method and protocol profiles are read in place from the local `../guide/` subtree.

The Field Guide release and commit recorded in `FIELDLAB-REGISTER.json` are the assessed baseline for the selected profiles, not a live method authority.

The local licensing map remains authoritative for this imported tree.
