# Monorepo note

This tree was imported from `IndelibleVivi/mcp-server-engineering-field-guide@87238302209d654358dd64eb3972677e4cacf256` for the MCP Boundary unified-workspace candidate.

Its historical `skill/mcp-server-engineering/` directory is frozen for this candidate as provenance, release evidence, validators, and reproducibility material. It is not active, not exposed by the MCP Boundary plugin manifest, and not a recommended installation path. The one active distributed skill is `../src/skills/mcp-boundary/` from the repository root.

This subtree is not a mirror of a still-authoritative upstream repository. The initial import is pinned in `../provenance/UPSTREAMS.lock.json`, and there is no re-import or synchronization path. The root `../AGENTS.md` owns the repository contract.

The local licensing map remains authoritative for this imported tree.
