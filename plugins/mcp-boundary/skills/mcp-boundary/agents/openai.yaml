interface:
  display_name: "MCP Boundary"
  short_description: "Complete MCP work on the live path"
  icon_small: "./assets/icon-400.png"
  icon_large: "./assets/mark.svg"
  brand_color: "#315BE8"
  default_prompt: "Use $mcp-boundary to trace the active MCP path, complete the requested change, and verify it at the boundary that owns the behavior."
policy:
  allow_implicit_invocation: true
